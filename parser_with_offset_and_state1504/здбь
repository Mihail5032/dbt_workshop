SAP всегда приравнивает salesamount = normalsalesamount для всех retailtypecode, или только для определённых? Потому что если для ВСЕХ — фикс простой (писать normalsalesamount вместо salesamount). Если только для некоторых — нужен список типов.
